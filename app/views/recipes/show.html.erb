<%# turbo frame used in each view to behave SPA like %>
<%# display the recipe with all infos %>
<turbo-frame id="react-like">
  <%= render partial: "shared/recipe_total", object: @recipe, as: :recipe   %>
</turbo-frame>
